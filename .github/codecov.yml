coverage:
  status:
    project:
      default:
        target: auto
        # Codecov should allow coverage to be reduced by a few percent
        threshold: 2%
        branches:
          - main
          - dev
    patch:
      default:
        informational: true
